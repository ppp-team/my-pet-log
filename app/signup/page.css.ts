import { style } from "@vanilla-extract/css";

export const container = style({ margin: 0, padding: "1.9rem 1.6rem", display: "flex", flexDirection: "column", alignItems: "center", maxWidth: "39.3rem" });

export const buttonWrapper = style({
  width: "100%",
  margin: "0.3rem 0",
});
