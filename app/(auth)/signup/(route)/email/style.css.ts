import { style } from "@vanilla-extract/css";

export const container = style({ padding: "0 1.6rem", display: "flex", flexDirection: "column", alignItems: "center" });

export const title = style({
  margin: "2rem auto 2.4rem",
  fontSize: "1.6rem",
  fontWeight: 700,
  textAlign: "center",
});
