import { style } from "@vanilla-extract/css";

export const container = style({ paddingLeft: "2rem" });
export const list = style({});
export const item = style({ width: "12.4rem!important", backgroundColor: "white" });
